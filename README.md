# Archivarius

Python, Flask, sqlite3, Html, Javascript, CSS.

**How to run:**
1. Unzip
2. Go to the unzipped parent directory
3. Open terminal
4. Type `flask run` and press enter
5. Go to the local link in the console response (something like "Go to `URL`)
